from sentence_transformer import Sentence_transformer

model = SentenceTransfomer(
"all-MiniLM-L6-V2"
)

def generate_embeddings(chunks):
texts=[
chunk['text']
for chunk in chunks
]
return model.encode(texts)
embeddings = generate_embeddings(
all_chunks
)

